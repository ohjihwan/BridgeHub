{
  "name": "rtc-server",
  "version": "1.0.0",
  "description": "WebRTC SFU + REST API + shared Socket.IO",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "dev":   "nodemon --watch src --ext mjs,json --exec \"node src/server.mjs\""
  },
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mediasoup": "^3.15.0",
    "socket.io": "^4.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}